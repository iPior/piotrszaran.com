---
import BaseLayout from './BaseLayout.astro';
import CategoryBadge from '../components/CategoryBadge.astro';
import StackPills from '../components/StackPills.astro';
import BackLink from '../components/BackLink.astro';
import { formatDate } from '../lib/utils';
import type { CollectionEntry } from 'astro:content';

interface Props {
  project: CollectionEntry<'projects'>;
  relatedBlog?: CollectionEntry<'blog'> | null;
}

const { project, relatedBlog } = Astro.props;
const { title, description, date, category, stack, liveUrl, githubUrl } = project.data;
---

<BaseLayout title={title} description={description}>
  <BackLink href="/" label="Back to home" />
  <article class="mt-6">
    <header class="mb-8">
      <div class="flex items-center gap-3 mb-3">
        <CategoryBadge category={category} />
        <time class="text-sm text-gray-500">{formatDate(date)}</time>
      </div>
      <h1 class="text-3xl font-bold mb-3">{title}</h1>
      <p class="text-gray-600 text-lg mb-4">{description}</p>
      {stack && stack.length > 0 && <StackPills stack={stack} />}
      <div class="flex gap-4 mt-4">
        {liveUrl && (
          <a href={liveUrl} target="_blank" rel="noopener noreferrer"
             class="text-sm text-blue-600 hover:underline">Live ↗</a>
        )}
        {githubUrl && (
          <a href={githubUrl} target="_blank" rel="noopener noreferrer"
             class="text-sm text-blue-600 hover:underline">GitHub ↗</a>
        )}
      </div>
      {relatedBlog && (
        <div class="mt-6 p-4 border border-gray-200 rounded-lg bg-gray-50">
          <p class="text-xs text-gray-500 mb-1 uppercase tracking-wide font-medium">Related</p>
          <a href={`/blog/${relatedBlog.slug}`} class="text-blue-600 hover:underline font-medium">
            Read the full write-up →
          </a>
        </div>
      )}
    </header>
    <hr class="border-gray-100 mb-8" />
    <div class="prose max-w-none">
      <slot />
    </div>
  </article>
</BaseLayout>
