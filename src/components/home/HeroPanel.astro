---
import HeroGrid from './hero/HeroGrid.astro';
import HeroHeader from './hero/HeroHeader.astro';
import HeroSocials from './hero/HeroSocials.astro';
import type { ProfileField, SocialLink } from './hero/types';

interface Props {
  firstName: string;
  lastName: string;
  role: string;
  profileImageSrc?: string;
  profileImageAlt?: string;
  profileFields?: ProfileField[];
  coreStack?: string[];
  socialLinks?: SocialLink[];
}

const defaultProfileFields: ProfileField[] = [
  { key: "status", value: "Open to full-time opportunities" },
  {
    key: "education",
    value:
      "Honors Bachelor of Science<br />University of Toronto, 2023",
  },
  { key: "currently", value: "&mdash; update this field" },
];

const defaultCoreStack = ["TypeScript", "Node.js", "React", "PostgreSQL"];

const defaultSocialLinks: SocialLink[] = [
  { label: "GitHub", href: "https://github.com/piotrszaran", icon: "github" },
  { label: "LinkedIn", href: "https://www.linkedin.com/in/update-this-profile", icon: "linkedin" },
  { label: "Email", href: "mailto:update-this@email.com", icon: "email" },
];

const {
  firstName,
  lastName,
  role,
  profileImageSrc,
  profileImageAlt = `${firstName} ${lastName} profile photo`,
  profileFields = defaultProfileFields,
  coreStack = defaultCoreStack,
  socialLinks = defaultSocialLinks,
} = Astro.props;
---

<section class="pt-9">
  <HeroHeader
    firstName={firstName}
    lastName={lastName}
    role={role}
    profileImageSrc={profileImageSrc}
    profileImageAlt={profileImageAlt}
  />
  <HeroSocials socialLinks={socialLinks} />
  <HeroGrid profileFields={profileFields} coreStack={coreStack} />
</section>
