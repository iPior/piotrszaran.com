---
interface Props {
  firstName: string;
  lastName: string;
  role: string;
  profileImageSrc?: string;
  profileImageAlt: string;
}

const { firstName, lastName, role, profileImageSrc, profileImageAlt } =
  Astro.props;
---

<div class="mb-2.5 text-xs text-site-dim">
  <span class="text-site-mauve">export</span>
  <span class="text-site-mauve">const</span> developer
  <span class="text-site-overlay">=</span>
  <span class="text-site-overlay">{"{"}</span>
</div>
<div>
  <div class="flex items-start gap-4 sm:gap-6 text-4xl leading-none sm:text-6xl md:text-7xl lg:text-8xl">
    {
      profileImageSrc && (
        <img
          src={profileImageSrc}
          alt={profileImageAlt}
          class="mt-[0.05em] h-[1.9em] w-[1.9em] shrink-0 rounded-2xl border border-site-border object-cover"
          loading="eager"
          decoding="async"
        />
      )
    }
    <h1 class="font-bold tracking-tight text-site-text">
      <span class="text-site-blue">{firstName}</span><br />
      <span class="font-normal text-site-yellow">{lastName}</span>
    </h1>
  </div>
  <div class="mt-2.5 text-sm text-site-dim">
    <span class="italic text-site-dim">{"}"} // {role}</span>
  </div>