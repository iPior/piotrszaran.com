---
import { formatDate } from '../lib/utils';

interface Props {
  slug: string;
  title: string;
  date: Date;
  description: string;
  readingTime?: number;
  tags?: string[];
}

const { slug, title, date, description, readingTime, tags } = Astro.props;
---

<a href={`/blog/${slug}`}
   class="block border border-site-border bg-site-surface rounded-lg p-5 hover:border-site-blue transition-colors">
  <div class="flex items-center gap-3 text-xs text-site-dim mb-2">
    <time datetime={date.toISOString()}>{formatDate(date)}</time>
    {readingTime && (
      <>
        <span aria-hidden="true">·</span>
        <span>{readingTime} min read</span>
      </>
    )}
  </div>
  <h3 class="font-semibold text-lg mb-1 text-site-text">{title}</h3>
  <p class="text-site-subtext text-sm">{description}</p>
  {tags && tags.length > 0 && (
    <div class="flex flex-wrap gap-1.5 mt-3">
      {tags.map((tag) => (
        <span class="text-xs border border-site-border rounded-full px-2 py-0.5 text-site-dim bg-site-surface-hover">
          #{tag}
        </span>
      ))}
    </div>
  )}
</a>
