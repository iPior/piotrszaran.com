---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a href={href} class="inline-flex items-center gap-1 text-sm text-site-dim hover:text-site-text">
  ← {label}
</a>
