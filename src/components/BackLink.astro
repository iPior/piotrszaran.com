---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a href={href} class="inline-flex items-center gap-1 text-sm text-gray-500 hover:text-gray-900">
  ← {label}
</a>
