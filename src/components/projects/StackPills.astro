---
interface Props {
  stack: string[];
}

const { stack } = Astro.props;
---

<div class="flex flex-wrap gap-1.5">
  {stack.map((tech) => (
    <span class="text-xs bg-site-surface-hover text-site-peach border border-site-border rounded px-2 py-0.5">
      {tech}
    </span>
  ))}
</div>
